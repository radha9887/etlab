# ETLab - Docker Environment Variables
# Copy this file to .env and customize for your deployment
#
# Usage:
#   cp .env.example .env
#   docker-compose up -d

# ===========================================
# Database Configuration
# ===========================================
# Default: SQLite (good for single-node deployment)
DATABASE_URL=sqlite+aiosqlite:///./data/etlab.db

# PostgreSQL example:
# DATABASE_URL=postgresql+asyncpg://user:password@postgres:5432/etlab

# MySQL example:
# DATABASE_URL=mysql+aiomysql://user:password@mysql:3306/etlab

# ===========================================
# Application Settings
# ===========================================
APP_ENV=production
LOG_LEVEL=INFO

# ===========================================
# JWT Authentication (IMPORTANT FOR PRODUCTION!)
# ===========================================
# Generate a secure key: openssl rand -base64 32
# If not set, sessions will be lost on container restart
JWT_SECRET_KEY=your-secure-secret-key-here

# ===========================================
# Spark Configuration (Local Mode)
# ===========================================
# Default local mode - runs Spark inside the container
SPARK_MASTER=local[*]
SPARK_APP_NAME=ETLab
SPARK_DRIVER_MEMORY=2g
SPARK_EXECUTOR_MEMORY=2g

# ===========================================
# Apache Livy (Remote Spark Clusters)
# ===========================================
# Use Livy to connect to:
#   - AWS EMR
#   - YARN clusters
#   - Kubernetes Spark
#   - Standalone Spark clusters
#
# Example for EMR:
# LIVY_URL=http://your-emr-master-dns:8998
#
# Example for local Livy:
# LIVY_URL=http://livy:8998

LIVY_URL=

# ===========================================
# Databricks (Optional)
# ===========================================
# Connect to Databricks workspace for code execution
#
# DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
# DATABRICKS_TOKEN=dapi-your-token-here
# DATABRICKS_CLUSTER_ID=your-cluster-id

DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_CLUSTER_ID=

# ===========================================
# Airflow Integration (for docker-compose.full.yml)
# ===========================================
# These are auto-configured in docker-compose.full.yml
# AIRFLOW_HOST=http://airflow:8080
# AIRFLOW_DAGS_PATH=/app/airflow/dags
